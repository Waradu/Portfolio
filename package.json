{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "start": "node .output/server/index.mjs",
    "deploy": "bun install && bun run build && bun run start",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^3.14.1592",
    "nuxt-build-cache": "^0.1.1",
    "sass": "^1.81.0",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0",
    "wrdu-keyboard": "^1.1.1"
  },
  "devDependencies": {
    "nuxt-particles": "^0.3.0"
  }
}
